FROM ubuntu:22.04

RUN apt-get update && apt-get install -y --no-install-recommends \
		build-essential cmake git python3 python3-dev python3-pip pkg-config \
		libssl-dev libsqlite3-dev zlib1g-dev libbz2-dev libxml2-dev \
		libboost-all-dev ca-certificates wget && rm -rf /var/lib/apt/lists/*


